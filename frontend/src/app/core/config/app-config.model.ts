/** Runtime configuration loaded from /config/app-config.json at startup. */
export interface AppConfig {
  /** Base URL of the SpiceSibyl backend API (e.g. http://localhost:8000/api/v1) */
  apiUrl: string;
}
